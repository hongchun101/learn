# Chapter 10 — Higher-Kinded Types

`F<_>` and natural transformations `∀α. F α → G α`.

## Goal

- Model kinds `*`, `* → *`, `(*,*) → *`.
- Work through a couple of `F<G<>>` patterns.

## Files

- `kinds.ts`, `transformations.ts`, `__tests__/chapter.test.ts`
