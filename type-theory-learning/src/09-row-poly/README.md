# Chapter 09 — Row Polymorphism

Records and effect-polymorphism via rows.

## Goal

- Parse & infer `{ a: Int, b : Bool | ρ }` (open rows).
- Closed rows end with `·` (empty).

## Files

- `ast.ts`, `infer.ts`, `__tests__/chapter.test.ts`
