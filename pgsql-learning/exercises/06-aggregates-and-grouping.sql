-- Exercise 06 — Aggregates and Grouping
-- Given visits(visitor_id, page, duration_sec), return per page:
--   visits_total, long_visits (duration >= 30), mean_dur, p95_disc, sorted_top_pages view.

-- Q1: write the per-page query with FILTER clauses.
-- Q2: write a ROLLUP(page) emitting a grand total row at the end.
-- Q3: select the median duration overall.
