[package]
name = "ch01-threads"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Ch01 — std::thread, sync primitives, mpsc, OnceLock, Barrier"

[dependencies]
parking_lot = { workspace = true }

[lib]
path = "src/lib.rs"
