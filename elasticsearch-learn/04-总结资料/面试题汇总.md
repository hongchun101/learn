# Elasticsearch 面试题汇总(2026 精编)

> 涵盖 入门 / 进阶 / 高级 / 专家 4 个级别。
> 每题有"**考察点**"提示,先想再看答案。

---

## 入门 10 题

### Q1. Elasticsearch 是什么?与 MySQL / MongoDB 有什么区别?

**答**:ES 是基于 Lucene 的 **分布式搜索与分析引擎**,近实时(NRT),用 RESTful API。
与 MySQL(MRDB)区别:

| 维度 | MySQL | ES |
| --- | --- | --- |
| 核心能力 | 事务(ACID) | 全文搜索 + 聚合分析 |
| 数据模型 | 表 / 行 / 列 | index / doc / field |
| 倒排索引 | 无(Fulltext 慢) | 内置 |
| 事务 | 强 | 弱(单文档) |
| 容量 | GB~TB | TB~PB |
| 适用 | 业务写 | 搜索 / 日志 / 分析 |

MongoDB 是文档型库,介于两者之间。

---

### Q2. 什么是倒排索引?

**答**:正向索引是 `文档 → 内容`,倒排索引是 `term → 文档列表`。
例:3 篇文档,倒排索引记录每个词出现在哪些文档 + 词频 + 位置(用于 phrase / 评分)。

> Lucene 用 FST + 跳表 + postings list 实现。

---

### Q3. ES 集群中 cluster / node / index / shard / document 是什么关系?

**答**:

- **Cluster**:集群,由多个 node 组成。
- **Node**:一个 ES 进程。
- **Index**:一类文档的集合,逻辑概念。
- **Shard**:index 物理切分单位,每个 shard 是一个 Lucene index。
- **Document**:一条 JSON,index 的最小单位。

---

### Q4. 什么是 primary shard 与 replica?创建后能改吗?

**答**:

- **primary shard**:主分片,负责写;创建 index 时定,不可改。
- **replica**:副本,从 primary 复制,可随时改。
- 主副本不在同一节点(强约束)。

> 改 primary 数 = reindex(用 shrink / split 可有限扩)。

---

### Q5. 解释 NRT(近实时)。写入后多久可搜?

**答**:`refresh_interval`(默认 1s)把内存中的 segment 刷到 filesystem cache,生成新的可搜 segment。
强制立即可搜:`POST /index/_refresh`(测试用)。

---

### Q6. PUT 与 POST 写文档有什么区别?

**答**:

- `PUT /index/_doc/<id>`:**必带 id**,可覆盖(幂等)。
- `POST /index/_doc`:**自动生成 id**,不覆盖(非幂等)。
- 局部更新用 `POST /_update/<id>`(底层 read-then-replace)。

---

### Q7. _bulk 是什么?为什么生产必用?

**答**:`_bulk` 一次提交多条写入/更新/删除请求(每条两行 NDJSON,delete 一行)。
生产必用原因:减少 HTTP round-trip,降低每个文档的协调开销。
建议批次 5~15 MB,过大反而 OOM。

---

### Q8. match 和 term 有什么区别?

**答**:

- `match`:会分词(text 字段),适合全文搜索。
- `term`:**不分词**,精确匹配。匹配 text 字段要 `.keyword`。

---

### Q9. ES 如何做乐观锁?

**答**:通过 `if_seq_no` + `if_primary_term` 防止并发覆盖。
不匹配 → 返回 409,客户端重试或回滚。
`_version` 从 7.x 起 deprecated。

---

### Q10. text 和 keyword 字段有什么区别?

**答**:

- `text`:分词 + 倒排,全文搜索。
- `keyword`:不分词,精确匹配 + 聚合 + 排序。
- 多字段:`text` 配 `fields.raw: keyword` 同时具备两种能力。

---

## 进阶 15 题

### Q11. 写出 text + keyword + numeric + date + geo_point 的 mapping 例子。

**答**:见 [第 5 章 Mapping](../01-进阶篇/05-Mapping与字段类型详解.md)。要点:

- text 配 ik_max_word 索引 / ik_smart 搜索。
- 金额用 scaled_float。
- 经纬度用 geo_point。

---

### Q12. 怎么用 IK?`ik_smart` vs `ik_max_word`?

**答**:IK 是中文分词器。

- `ik_max_word`:细粒度,切分多,索引用,召回全。
- `ik_smart`:粗粒度,搜索用,精度高。

> 索引用 `max_word`、搜索用 `smart` 是生产经验。

---

### Q13. bool query 中 must / should / filter / must_not 的区别?

**答**:

- `must`:必须,打分。
- `should`:可选,打分;无 must 时至少 1 个。
- `filter`:必须,**不打分**,可缓存。
- `must_not`:反向,不打分。

> 能 filter 的就 filter,效率高。

---

### Q14. 什么是 query context 与 filter context?

**答**:

- **Query context**:会算 `_score`(相关性)。
- **Filter context**:只判断匹配 / 不匹配,不打分,可缓存,效率高。

---

### Q15. multi_match 的 type 区别?电商用什么?

**答**:

- `best_fields`(默认):取单字段最高分。
- `most_fields`:多字段分数求和。
- `cross_fields`:跨字段视作整体。
- `phrase` / `phrase_prefix` / `bool_prefix`:短语 / 前缀。

电商"商品搜索"通常用 `best_fields`(title 命中更重要)。

---

### Q16. 怎么深分页?from+size 限制多少?

**答**:`from + size` 默认 ≤ `index.max_result_window`(10000)。
超了会报错或极慢。改用:

- `search_after` + 稳定 sort。
- PIT(point in time)。
- 不支持跳页,只能顺序翻。

---

### Q17. terms / date_histogram / composite agg 怎么选?

**答**:

- `terms`:固定字段分桶,top N(精度问题)。
- `date_histogram`:时间分桶。
- `composite`:分桶分页(高基数 / 全分桶)。

---

### Q18. 聚合结果为什么"不精确"?

**答**:`terms` 在每个 shard 取 top N,再合并。
某 term 在 shard A 是 top5、shard B 是 top20,合并时可能漏。
解决:调大 `shard_size`(1.5~2x size),或 `composite`。

---

### Q19. 解释 nested 与 object 的区别。

**答**:ES 默认 `object` 字段扁平化(数组里所有 `name` 放一字段),**丢失关联**。
`nested` 把每条数组项存为独立隐藏文档,可独立查询。
代价:索引体积大,查询开销大。

---

### Q20. dense_vector 是什么?用在哪?

**答**:`dense_vector` 存浮点向量,支持 **kNN** 检索(ANN 算法)。
用于 RAG / 语义搜索 / 图像检索。
`dims` 维度越大越准,但内存与计算成本越高。

---

### Q21. highlight 内部怎么工作?代价?

**答**:用 `unified` / `plain` / `fvh` 重新跑分词,从 source 取片段包 `<em>`。
性能成本:需要重新访问 source + 计算。
`fvh`(fast vector highlighter)需 `term_vector: with_positions_offsets`,更快但写入大。

---

### Q22. 什么是 completion suggester?与 search_as_you_type 区别?

**答**:

- **completion**:FST 实现,极快,但只能前缀。
- **search_as_you_type**:多个 subfield 支持中间词匹配,较灵活。

---

### Q23. 怎么排查一个"搜不到"的 query?

**答**:

1. `_analyze` 验证分词。
2. `validate/query?explain=true` 验证 DSL。
3. `explain=true` 看每个文档的 _score 公式。
4. profile=true 看耗时阶段。
5. 检查 mapping(字段是否 keyword?分词器对吗?)。
6. 查是否 data stream 不允许 update。

---

### Q24. _source 是什么?关掉会怎样?

**答**:存原始 JSON。关掉:

- 失去 update / reindex 能力。
- 失去 highlight。
- 失去 painless script 读源。
- 节省 ~10%~30% 存储。

> 一般不关。

---

### Q25. dynamic mapping 策略?生产用哪个?

**答**:

- `true`(默认):自动加字段,易爆炸。
- `false`:能存但不索引。
- `strict`:出现新字段直接报错。
- `runtime`:作为运行时字段。

> 生产推荐 `strict` 或配合 dynamic_templates 显式管理。

---

## 高级 15 题

### Q26. 解释 BM25 公式,各部分含义。

**答**:

$$
\text{score} = \sum_{t \in Q} IDF(t) \cdot \frac{tf(t, D) \cdot (k_1+1)}{tf(t, D) + k_1 \cdot (1-b+b \cdot |D|/\text{avgdl})}
$$

- `tf`:词频。
- `IDF`:逆文档频率,稀有词权重大。
- `|D|`:文档长度;`avgdl`:平均长度。
- `k1`(默认 1.2):TF 饱和控制。
- `b`(默认 0.75):长度归一化。

---

### Q27. function_score 怎么融合业务权重?

**答**:用 `functions` 数组组合 `weight` / `field_value_factor` / `gauss` 等,通过 `score_mode` 合并函数分,再 `boost_mode` 与原 query 分组合(常用 multiply / sum)。

例:相关性 × 销量 log × 时间衰减。

---

### Q28. ES 写入流程(translog / refresh / flush)。

**答**:

1. 写 translog(同步落盘)。
2. 写内存 buffer。
3. refresh(默认 1s):buffer → filesystem cache(可搜)。
4. 每 30 分钟 / translog 大:flush → segment + 切 translog。

> translog 保证宕机不丢;segment 不可改,只能 merge。

---

### Q29. 单 shard 多大合适?为什么?

**答**:**10~50 GB**。
太小:分片多,资源浪费,merge 慢。
太大:recovery 慢,merge 慢,GC 慢。

---

### Q30. 副本数怎么选?写多 / 读多场景。

**答**:

- 数据可靠性:副本 ≥ 1。
- 写密集:副本 0 写,再加(写时副本是 bottleneck)。
- 读密集:副本多,搜索可分摊。
- 集群规模:副本 ≤ 节点数 - 1。

---

### Q31. 解释 ILM 的 4 阶段。

**答**:

- **Hot**:写入密集,SSD。
- **Warm**:读多写少,降配。
- **Cold**:极少读,HDD / 普通磁盘。
- **Frozen**:对象存储(S3),本地几乎不占。

`min_age` 从 **rollover 时间**算起。

---

### Q32. Data Stream 是什么?与普通 index 区别?

**答**:**8.x 新**,append-only 场景(日志 / 指标)。
内部对应多个 backing index(`-000001`),自动 rollover。
- 写必须用 `_create`。
- 拒绝 `_doc` / `_update` / `_delete`。
- 简化开发,适合"事件流"。

---

### Q33. 集群 master 选举怎么防脑裂?

**答**:

- 至少 3 个 master eligible 节点(odd)。
- majority 算法:`floor(N/2)+1`。
- 网络分裂超过 majority 才允许选主。
- ES 7.x 起自动算 `minimum_master_nodes`,无需手动设。
- 避免 GC / 磁盘满导致假死。

---

### Q34. cluster state 是什么?多大合适?

**答**:集群全局元数据(index / mapping / settings / shard 位置)。
master 节点维护,通过 2PC 同步。
state 越大(单 index mapping 巨大、shard 数多)→ master 内存 / 网络压力大。
优化:减少 index 数(用数据流 / 滚动),简化 mapping。

---

### Q35. 写入流程为什么副本数多会拖慢?

**答**:主分片写完 → 转发到所有副本 → 副本成功 → 主分片成功。
副本数 = 副本转发次数 + 副本刷盘次数。
副本越多,latency 越高,出错率越高。

---

### Q36. search 流程的两阶段是什么?

**答**:

- **Query phase**:每个 shard 内部算 top-K(文档 id + score),返回给协调节点。
- **Fetch phase**:协调节点 merge + 全局排序后,回各 shard 取 `_source` + highlight。

---

### Q37. ES 调优 checklist。

**答**:

- 写:bulk 5~15MB / refresh 30s+ / translog async / 副本 0 写。
- 查:filter 优先 / search_after / routing / request_cache。
- 索引:单 shard 10~50G / 角色分离 / ILM。
- JVM:Xms = Xmx = 物理 / 2(≤ 64G)或 32G / G1GC / 关 swap。
- 集群:3 master / odd / 慢日志 / SLM。

---

### Q38. JVM heap 为什么建议 ≤ 32G?

**答**:超过 32G,指针压缩失效(Compressed Oops,基于 4G 寻址),JVM 内存翻倍,GC 显著变慢。
32G + 强 FS Page Cache 是 Lucene 黄金搭档。

---

### Q39. 节点 OOM 怎么定位?

**答**:

1. `/_nodes/hot_threads` 看占用最高线程。
2. heap dump:`jmap -dump:live,format=b,file=heap.bin <pid>`。
3. MAT / VisualVM 分析大对象。
4. 常见:fielddata 爆 / 大聚合 / 深分页 / 大量并发 query。

---

### Q40. 怎么定位"集群写入慢"?

**答**:

1. `/_cat/thread_pool/write?v&h=node_name,active,queue,rejected` 看写队列。
2. `/_nodes/stats/indices/indexing` 看 index throttle 时间。
3. 慢日志 `logs/*_indexing_slowlog.log`。
4. 检查 `index.merge.*` 是否被大 segment 卡住。
5. disk IO:`iostat -x 1`。
6. 常见原因:副本多 / 段合并 / refresh 太频繁。

---

## 专家 20 题

### Q41. CCR 与 CCS 的核心区别?

**答**:

- **CCR**:跨集群**复制**数据(写一次,远端同步),Platinum。
- **CCS**:跨集群**搜索**(查询时聚合多集群结果),基础免费。

---

### Q42. ES 8.x 默认开启 security 吗?为什么?

**答**:**是**。8.x 起默认开启 xpack.security + HTTPS,要求账号密码登录。
原因:安全合规、默认安全。

---

### Q43. API Key 与密码的区别?服务间用哪个?

**答**:

- **密码**:长期,与用户绑定;权限粒度粗;泄露影响用户所有操作。
- **API Key**:可设过期 / 细粒度权限;吊销灵活;服务间调用首选。

---

### Q44. DLS / FLS 是什么?场景?

**答**:

- **DLS**(Document Level Security):通过 query 限定"能看哪些文档",类似 SQL row-level security。多租户场景。
- **FLS**(Field Level Security):限定"能看哪些字段",屏蔽敏感字段(token / 身份证)。
- 都需要 X-Pack Platinum。

---

### Q45. 自定义分词器插件怎么写?生命周期?

**答**:实现 `AnalysisPlugin` 接口,注册 `analyzer` / `tokenizer` / `filter`。
Gradle + elasticsearch.esplugin 插件构建,产出 zip,`elasticsearch-plugin install`。
插件目录在 `$ES_HOME/plugins/<name>/`。
8.x 默认要求插件签名。

---

### Q46. 解释 ES 在 RAG 中的角色。

**答**:RAG = 检索增强生成。ES 作为"检索引擎":

- BM25 找精确词命中。
- dense_vector + knn 找语义近。
- 混合召回 → top K → LLM 拼成 prompt 生成答案。

ES 提供"原文依据",可溯源。

---

### Q47. 索引模板(Index Template)与 Component 模板。

**答**:

- **Index Template**:index 创建时套用,8.x 推 `composable` 写法。
- **Component Template**:可复用片段,被 index template 引用(`composed_of`)。

> Component 模板管 settings,mapping 单独管,组合用。

---

### Q48. 集群 red 的处理流程。

**答**:

1. `GET /_cluster/health` 确认。
2. `GET /_cluster/allocation/explain` 看 unassigned 原因。
3. 常见:
   - 磁盘满 → 清 / 扩。
   - 配置限分片 → 改 `cluster.routing.allocation.*`。
   - 节点 down → 重启 / 替换。
4. 实在无法恢复:`POST /_cluster/reroute` 强制分配(丢数据风险)。

---

### Q49. 解释 searchable snapshot。

**答**:把冷数据放对象存储(S3),本地不存,查询时按需拉 segment 到本地 cache。
7.12+ 引入,frozen tier 的核心。
适合"低频但要查"的合规数据。

---

### Q50. 解释 shard allocation explain。

**答**:`GET /_cluster/allocation/explain` 返回每个 unassigned shard 的"为什么没分出去"。
- 磁盘阈值超
- 节点过滤不满足
- 分配被禁用
- 节点离线
- 版本不兼容

> 排错的"第一道 API"。

---

### Q51. ES 与 ClickHouse 选哪个?

**答**:

- **ES**:全文搜索 / 日志 / 文档 / 向量,长文本模糊查。
- **ClickHouse**:OLAP 大宽表聚合,列存极致压缩,毫秒级聚合亿级。

> 日志 + 简单统计用 ES;纯聚合(广告效果、用户画像)用 ClickHouse。

---

### Q52. ES 与 OpenSearch 区别?

**答**:

- OpenSearch 是 AWS 在 7.10.2 基础上的 fork,Apache 2.0 协议。
- API 高度兼容,但已分化(部分新功能 / 商业特性差异)。
- 选型:用 Elastic 服务 / 商业功能选 ES;想要纯开源 / 避免 vendor lock 选 OpenSearch。

---

### Q53. 解释 shard size / shard count / 节点数 的容量公式。

**答**:

- 数据量 1TB,期望单 shard 30GB → 主分片数 = 33。
- 节点数 5,副本 1 → 实际 66 个分片(33 主 + 33 副本),每节点 ~13 个分片。
- 每节点磁盘 = 数据 × (1 + 副本) / 节点数 = 1TB × 2 / 5 = 400GB。
- 预留 30%~50% → 实际配 600GB。

---

### Q54. 写后立刻查不到?为什么?

**答**:写入走"buffer → refresh",默认 1s 才可搜。
强制立即可搜:`POST /index/_refresh`。
生产调整 `index.refresh_interval`,平衡实时性 vs 写入性能。

---

### Q55. 怎么验证我的索引设计是对的?

**答**:

1. `GET /index/_mapping` 看是否符合预期。
2. `_analyze` 验证分词结果。
3. `validate/query?explain=true` 验证 DSL 命中期望字段。
4. 真实查询 + `explain=true` 看打分构成。
5. 写一个 `_profile` 跑压测,看瓶颈。
6. 写 A/B 切流对比 CTR / 转化。

---

### Q56. 滚动升级流程。

**答**:

1. 禁 shard 分配:`PUT /_cluster/settings {"transient": {"cluster.routing.allocation.enable": "primaries"}}`。
2. 停 / 升级一个节点。
3. 等集群状态变绿,再下一个。
4. 全做完,恢复默认分配。

> 跨大版本(7 → 8)需先升到中间版。

---

### Q57. ES 怎么防"上游字段爆炸"?

**答**:

- `dynamic: strict` 直接拒。
- `dynamic: false` + ingest pipeline 过滤字段。
- `dynamic_templates` 把所有 string 限定为 keyword。
- 在 ES 前置一层网关 / Kafka 做 schema 校验。

---

### Q58. 怎么决定用 routing?

**答**:

- **多租户**:按 `tenant_id` routing,查询只走 1 个 shard。
- **业务隔离**:大品牌 / 大商户自营 routing。
- 风险:租户倾斜 → 热点 shard → 拆租户 / 加节点。
- 评估:80% 流量是否集中少数租户。

---

### Q59. ES 集群 100 个节点,master 怎么设?

**答**:

- **3 个 master only 节点**(2 核 8G,无 data 角色)。
- data 节点按业务 / 硬件分角色(ingest / hot / warm / cold)。
- coordinating 节点单独部署(扛高并发查询)。
- 用 `node.roles` 显式声明。

---

### Q60. 跨集群场景选什么方案?

**答**:

| 场景 | 方案 |
| --- | --- |
| 异地多活读 | CCR + CCS |
| 异地多活写(双写) | 业务双写 + 异步对账 |
| 灾备 | CCR / Snapshot |
| 跨集群聚合报表 | CCS |
| 跨集群数据迁移 | CCR / reindex remote |

---

## 加分 10 题(架构设计 / 场景题)

### Q61. 设计一个 100 亿条订单的搜索系统。

**答**:

- 索引:按 `order_id` routing(同用户订单集中)。
- 按时间 rollover:`orders-2024-01`,ILM hot/warm/cold 1 年删。
- 副本 1,~30~50 GB / shard,主分片 100+。
- 写:业务双写 + 异步校验(MySQL 主,ES 索引)。
- 查:filter + function_score,深分页 search_after。
- 监控:每个 shard latency / heap / queue 告警。

---

### Q62. 设计一个全公司日志平台。

**答**:

- Beats 全量采,送 Kafka,Logstash 解析。
- ES Hot 1 天 + Warm 7 天 + Cold 30 天 + Frozen 1 年。
- Spaces 隔离团队。
- Watcher 告警(错误率 / 慢请求 / 关键日志)。
- ILM + SLM 自动滚动 + 备份。
- Alerting 接入飞书 / 钉钉。

---

### Q63. 如何从 0 设计一个电商搜索的"权重融合"?

**答**:

1. 基础相关性 BM25,字段加权 `title^3, tags^2`。
2. function_score 加:
   - 销量(log1p,避免爆大)。
   - 评分(sqrt)。
   - 新品(gauss 时间衰减)。
   - 官方 / 自营(weight)。
3. `boost_mode: sum` 合并业务与相关性。
4. 上线前 A/B 对比,CTR / 转化。

---

### Q64. ES 写入瓶颈在哪里?怎么压测?

**答**:

- 写瓶颈:副本转发 / segment merge / disk IO。
- 压测工具:esrally 跑 geonames / nyc_taxis;wrk / vegeta 自定义。
- 调优:副本 0 写 / bulk 5~15MB / refresh 30s / translog async / SSD。

---

### Q65. ES 与向量数据库(Milvus / Qdrant)的区别?

**答**:

- **ES** + dense_vector:一体化存储(结构化 + 向量),中等规模,knn + filter 强。
- **Milvus / Qdrant**:纯向量数据库,亿级向量,ANN 算法丰富,无结构化过滤。

> 业务上:既要全文也要语义 → ES hybrid;纯语义 → Milvus。

---

### Q66. 怎么在 ES 上做"长事务" / "并发写"?

**答**:

- ES 不支持 ACID 事务。
- 写并发:用 `_seq_no + _primary_term` 做乐观锁。
- 跨文档"事务":业务侧用 `try { 写多个 } catch { 回滚 }`,但部分成功不保证。

---

### Q67. 解释 shard recovery 流程。

**答**:节点加入 / 主分片丢失 → 从其他副本拉 segment。
阶段:
- **init**:初始化(建 translog)。
- **index**:拉 segment 验证。
- **verify_index**:对账。
- **translog**:replay 增量。
- **finalize** → done。

限速:`index.recovery.max_bytes_per_sec`。

---

### Q68. ES 8.x vs 7.x 重大变化。

**答**:

- 默认开启 Security / HTTPS。
- Mapping types 完全移除(6.x 开始)。
- ILM 改 Data Stream。
- 删 `_doc` 类型 8.x 已默认。
- Painless + runtime fields。
- dense_vector 强,k-NN 优化。
- ES 8.13 / 8.14 持续小迭代。

---

### Q69. 怎么从 0 选 ES 版本?

**答**:

- 生产:最新稳定大版本 +1~2 个月内(避免 bug 高发期)。
- 长期:跟官方大版本走(每 1~2 年一版)。
- 7.x → 8.x:必须先升 7.17(最后 7.x),再升 8.x。
- 强烈建议升级到 8.x 享受免费 alerting / SIEM / 性能优化。

---

### Q70. ES 在 AI 时代的位置?

**答**:

- ES 是 **"搜索 + 分析"** 基础设施。
- RAG 时代:BM25 + 向量混合召回,ES 提供"可溯源检索"。
- ES 不直接做 LLM,但提供"上下文素材库"。
- 与 LLM 配合:hybrid 检索 → top K → LLM 生成。

---

## 总结

> ES 面试要点:**基础扎实 + 性能调优 + 业务理解 + 端到端方案**。
> 建议准备 30~60 分钟,从"基础概念"到"故障排查"到"业务设计"逐层展开。

---

## 资深专家级(50K+ 场景题)

### Q71. 1PB 数据 + 5w QPS 该怎么设计 ES 集群?

**答**:

- **总数据量** ≈ 1 PB × 1.3(索引开销) × 2(副本) × 1.5(余量) = 4 PB 物理。
- **分层**:hot 10% / warm 30% / cold 40% / frozen 20%。
- **Hot** 节点 32C128G NVMe × 30(支撑 5w QPS 与实时写入)。
- **Warm** 节点 16C64G SSD × 30。
- **Cold** 节点 16C64G HDD × 20。
- **Frozen** 走 S3,无独立节点。
- **Master** × 3 独立;**coordinating** 节点 × 5 跑客户端接入。
- **总成本**(自建)≈ 80-120 万/月;(公有云)200 万+/月。

**考察点**:容量规划 + 成本 + 分层。

---

### Q72. 写一份零停机 schema 升级的步骤清单

**答**:
1. snapshot 备份。
2. 业务读写走别名(永远是别名)。
3. 准备 `v2` 索引,新 mapping。
4. `_reindex` + 限速 + 关闭 refresh,提升副本 0。
5. 完成后改副本 1 + 打开 refresh。
6. 业务层引入双写(`_id` + `version_type=external`),先 1% 灰度。
7. 全量双写 + 主读切到 v2。
8. 保留 7 天再删 v1。

**考察点**:reindex + 别名 + 双写一致性。

---

### Q73. ES 集群 RTO=1h, RPO=5min 怎么实现?

**答**:
- **RPO 5 min**:translog 每 5s fsync,主写 + 2 副本,CCR 异步跨 region 5s 内追上。
- **RTO 1h**:跨 region 预热集群 + 自动化切流 + runbook 演练。
- **关键**:每季度一次灾难恢复演练,实测 RTO。

---

### Q74. dense_vector 内存怎么估?

**答**:
- `int8_hnsw`:字节数 = `doc_count × dims × 1`。
- `hnsw(float32)`:字节数 = `doc_count × dims × 4`。
- `bbq_hnsw`:字节数 ≈ `doc_count × dims × 0.5` + rescoring 开销。
- 1 亿 doc × 1024 维 int8 ≈ 100 GB;bbq ≈ 50 GB。
- 务必 **单 shard < 1GB HNSW**。

---

### Q75. Painless 写 script_score 怎么避免 NPE?

**答**:
- 字段 missing 时 `doc['x'].value` 抛 NPE。
- 用三元 `doc['x'].size() > 0 ? doc['x'].value : default` 兜底。
- 优先用 `doc_values` 而非 `_source`。
- `params` 传常量,不让 painless 内做时间计算。

---

### Q76. bulk 失败 doc 如何处理?

**答**:
- 遍历 `bulk` 响应的 `items`,取 `error != null` 的项,写死信队列。
- **不要**整批重发,会引发版本冲突与数据重复。
- 死信 doc 由批处理任务补偿,可观测。

---

### Q77. 集群 red 怎么 5 分钟止血?

**答**:
1. `GET /_cluster/allocation/explain` 1 min。
2. 临时 `PUT /index/_settings {"index.blocks.write": true}`。
3. 查 unassigned 原因(节点 / 磁盘 / 调度)。
4. 若是副本损坏,**不要** 强制 reroute,优先恢复主分片数据。
5. 修根因(扩节点 / 清盘)→ 触发自动重平衡。

---

### Q78. ES JVM 32GB 上限的原理?

**答**:
- JVM 在 < 32 GB 用 **Compressed Oops**,对象指针 4 字节。
- 超 32 GB 失效,变 8 字节,**内存翻倍 + GC 慢**。
- ES 7.x 起默认 `-Xmx 32g`,生产不要超。
- 物理内存 64 GB 时,JVM 31 GB + filesystem cache 33 GB 是最佳配比。

---

### Q79. 怎么判断 query 该用 filter 还是 query context?

**答**:
- **filter context**:不影响打分、走 cache、适合 yes/no 判断(状态、范围、term)。
- **query context**:参与打分、适合"相关性匹配"(match、multi_match)。
- bool 子句:must 走 query,filter 走 filter,should 走 query。
- 不需要打分 → 全 filter,P99 立刻降到个位数 ms。

---

### Q80. ES vs ClickHouse 选型?

**答**:
- **ES**:强在文本/向量搜索与相关性,聚合在 100 亿级以下够用,生态全(ELK)。
- **ClickHouse**:强在 OLAP 聚合(列存 + 向量化),百亿级聚合秒级,文本搜索弱。
- 选型:
  - 文本/向量/多条件筛选 → ES。
  - 大宽表聚合分析 → ClickHouse。
  - 混合方案:ES 召回 + ClickHouse 聚合。

---

### Q81. ES vs OpenSearch 选型?

**答**:
- OpenSearch 是 ES 7.10.2 的 fork(2021),Apache 2.0 协议,AWS 主推。
- ES 8.x 是 SSPL/Elastic License,商业限制。
- 选型:
  - 商业生态、合规、可观测全家桶 → Elastic。
  - 避免锁定、完全开源、自托管 → OpenSearch。
  - API 高度兼容,迁移成本主要是插件与商业功能(ML、Alerting)。

---

### Q82. explain=true 在生产能用吗?

**答**:
- **不能** 用于生产,explain 关闭打分 cache,大量 trace 计算,单次开销 10-100×。
- 用 `profile=true` 找瓶颈段(query / rewrite / score / fetch / highlight),开销比 explain 小。
- 慢查询定位:慢日志 + profile 抽样 + `_tasks` 长任务监控。

---

### Q83. master 节点兼任 data 的危害?

**答**:
- 集群状态(Cluster State)更新会触发全集群推送,master 节点 IO/内存压力飙升。
- 高 QPS 写入时频繁 master 切换,集群 yellow 甚至 red。
- **正确做法**:3 master 独立 + data 节点只负责数据,协调节点独立承载查询入口。

---

### Q84. ES 如何处理时序数据膨胀?

**答**:
- **Data Stream + ILM**:写入 hot tier 1d → warm 7d → cold 30d → frozen 90d → delete 365d。
- **forcemerge** warm 阶段,segment 合并降低资源。
- **searchable snapshot** cold/frozen 走对象存储,降本 70%。
- **降副本**:warm/cold 阶段副本 1,甚至 0。

---

### Q85. ES 在 100w QPS 下怎么做?

**答**:
- **100w QPS** = 整集群 100w,单 query 100ms → 100k 并发。
- 入口需 **coordinating 节点 × 50+** 做负载均衡,无状态。
- 客户端多连接,HTTP/2,keepalive。
- 业务侧:filter context + query_cache + 限制返回字段。
- **不推荐** 100w QPS 走 ES,100w 适合前置 Cache(Redis/ES Query Cache)+ ES 后端兜底。

---

### Q86. ES search_after 与 scroll 区别?

**答**:
- **scroll**:维护游标上下文,服务器开销大,适合一次性全量导出。
- **search_after**:基于 sort 值翻页,无上下文,适合深分页 + 用户交互。
- 7.10+ 推荐用 **PIT(point in time) + search_after**,可保证一致快照下的翻页。

---

### Q87. ES segment merge 怎么调?

**答**:
- 默认 `index.merge.policy.max_merged_segment: 5GB`,`segments_per_tier: 10`。
- 写入密集场景调大 `max_merged_segment` 减少 merge 频次。
- 静态索引用 `forcemerge` 强制 1 segment,查询快但失去写入优化。
- 监控 `primaries.merges.current`,长期 > 3 表示 IO 跟不上。

---

### Q88. 怎么防止 ES 字段爆炸?

**答**:
- 强制 `dynamic: strict` 或 `false`。
- 设 `index.mapping.total_fields_limit: 2000`。
- 设 `index.mapping.depth.limit: 20`。
- `index.mapping.nested_fields.limit: 50`。
- 监控 `indices.mapping.total_fields` 与 `total_field_data_size`。

---

### Q89. ES 如何保证写入幂等?

**答**:
- 用 `_id` 固定,重复 PUT 同 _id 覆盖 = 幂等。
- 用 `version_type=external` + 业务时间戳,新版本号胜出。
- 用 `if_seq_no` + `if_primary_term` 做乐观锁。
- Kafka + ES 场景:消息带 `_id`,消费者去重,处理失败入死信。

---

### Q90. ES 写入抖动怎么定位?

**答**:
- 查 `indexing.index_failed` 是否 > 0。
- `/_nodes/stats/thread_pool/write` 看 queue 与 rejected。
- `/_cat/pending_tasks?v` 看集群状态任务。
- `/_nodes/hot_threads` 看热点。
- 看磁盘 IO(`iostat -x`),GC(`jstat -gc`)。

---

### Q91. ES 怎么和 Kafka 集成?

**答**:
- Kafka Connect + Elasticsearch Sink Connector(官方)。
- Logstash 消费 Kafka → ES。
- 自研消费者服务(Java/Python),批量 bulk 写 ES。
- 关键:at-least-once + 业务去重(用 _id);Kafka offset 由 bulk 成功 ACK 推进。

---

### Q92. ES 与 MySQL 数据同步?

**答**:
- **应用层双写**:简单,业务侵入。
- **CDC**:Canal/Debezium 订阅 MySQL binlog → Kafka → ES Sink。
- **Logstash JDBC input**:定时拉,延迟大,不推荐实时。
- **DataX / DTS**:阿里云方案,ETL 框架。
- 选型:实时性要求高 → CDC;容许分钟级 → 应用双写。

---

### Q93. 怎么测试 ES 的性能?

**答**:
- **esrally**:官方基准,内置 nyc_taxi / geonames / http_logs。
- **真实流量回放**:tcpdump 抓线上流量,用 Python 脚本回放。
- 关注指标:indexing throughput、query latency p50/p99、heap、CPU、磁盘 IO。
- 测试矩阵:数据量 × 副本 × 节点数 × query 复杂度。

---

### Q94. 怎么实现 ES 的灰度发布?

**答**:
- **索引级别**:双写 v1/v2,按 `user_id%100 < 灰度比例` 路由。
- **集群级别**:CCR 拉取部分索引,客户端按规则分流。
- **查询级别**:Linear Retriever 多个索引召回,加业务 tag 区分。
- 推荐索引级别灰度,简单可控。

---

### Q95. ES 集群迁移到新机房怎么做?

**答**:
- 方案 1:CCR 跨集群同步,验证一致后切流。
- 方案 2:snapshot 备份 → 目标集群 restore → 双写 → 切读。
- 方案 3:reindex from remote,适合跨集群单次迁移。
- 关键:迁移前后 1 周双写 + 抽样对账。

---

### Q96. ES 冷数据查询慢怎么办?

**答**:
- frozen tier 走 S3,延迟 5-10× 是正常,业务必须接受。
- 优化:`searchable_snapshot` cache 预热;提前 merge;副本 0 节省 IO。
- 业务策略:hot 索引建好覆盖 90% 查询,cold 仅做合规审计。
- 替代:把冷数据迁到 ClickHouse / Hive / 对象存储,按需加载。

---

### Q97. ES 安全合规怎么做?

**答**:
- 开启 xpack security,内置 RBAC。
- 文档/字段级 DLS/FLS 控权。
- 审计日志 `audit.log` 接入 SIEM。
- 传输 TLS + 静态加密(KMS)。
- GDPR 合规:删除用户数据用 `delete_by_query` + 物理删除。
- 凭据管理:API Key + 定期轮换,严禁 long-lived 密码。

---

### Q98. ES 怎么支持多语言?

**答**:
- 中文:IK 分词(ik_smart / ik_max_word)+ 同义词。
- 英文:standard + lowercase + asciifolding。
- 日文:Kuromoji(形態素解析)。
- 阿拉伯:arabic analyzer。
- 多语:用 `icu_analyzer`。
- Embedding:多语模型(bge-m3 / mE5 / Cohere multilingual v3)。
- 避免一索引多语:分语种建索引,查询时 union + boost。

---

### Q99. ES 在国内 50K 面试中常考的"高级认知"问题?

**答**:
- "**ES 不是数据库**:别当 MySQL 用,事务、join、二级索引都不是它的强项。"
- "**Lucene 是核心**:理解 segment / merge / translog 才有调优直觉。"
- "**集群状态是瓶颈**:master 节点的 cluster state 不要太大,否则推送阻塞。"
- "**冷热分层是降本关键**:不分层,月账单翻 3 倍。"
- "**向量不是万能**:BM25 + 向量才是生产答案。"
- "**监控先于优化**:没有指标,调优就是赌博。"

---

### Q100. 50K 专家面试的"5 大开放题"该如何回答?

**答**:
1. **设计千万级商品搜索**:mapping → analyzer → RRF → 监控 → 容量。
2. **生产故障复盘**:现象 → 定位 → 止血 → 根因 → 长期 → 反思。
3. **跨 region 灾备**:CCR + DNS + RTO/RPO + 演练频次。
4. **降本方案**:分层 + 副本 + 量化 + 业务瘦身。
5. **AI 时代 ES 位置**:hybrid 检索 + LLM = RAG 标准答案。

回答公式:**场景 + 选型 + 量化 + 风险 + 备选**。

---
下一节:[FAQ 与常见坑 →](./FAQ常见坑.md)
